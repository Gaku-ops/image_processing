💡 次回以降、最初から立ち上げる際の手順まとめ
今後、PCを再起動したあとなどに本システムを立ち上げる場合の手順です。

1. 描画ソフト（XLaunch）の起動
※ これを忘れると「表示先が見つからない」というエラーになります。

Windowsのスタートメニューから「XLaunch」を起動します。
そのまま「次へ」を2回押します。
Extra settings の画面で 「Disable access control」にチェック を入れます。
「次へ」→「完了」を押します。 （タスクトレイに黒いXのアイコンが出ていれば準備OKです）
2. アプリケーション（Docker）の起動
コマンドプロンプトを開きます。
システムのフォルダに移動します。
bash


cd c:\Users\USER\Documents\smart_agri
以下のコマンドでシステムを一斉起動します。（※2回目以降は --build は不要です）
docker-compose build プログラムを変更したときのみ再ビルドが必要
docker-compose up
3. 画像の読み込みとパラメータ調整
エクスプローラーで c:\Users\USER\Documents\smart_agri\data フォルダを開きます。
処理したい画像をそこに入れます（複数枚入れてもOKです）。
すると、先ほど起動したGUI画面のほかに**「Realtime Preview」という画像ウィンドウ**が自動でポップアップします。
GUI画面で「HSV」や「ExG」などのラジオボタンを切り替えたり、スライダーを動かしてみてください。
動かした瞬間に、プレビューウィンドウ側の画像の色（抽出状態）がリアルタイムに変化するのが確認できます！
また、このシステムを別のPCに移植する際は、今回作成した smart_agri フォルダをそのままUSB等でコピーし、そのPCでDockerとXLaunch（Linuxの場合は不要）を入れて同じコマンドを打つだけで全く同じように動きます。






1. HSV手法（指定した色をカッチリ抜き出す）
HSVは、画像を「色合い（Hue）」「鮮やかさ（Saturation）」「明るさ（Value）」の3つの要素に分けて考える手法です。

仕組み: 「この色からこの色まで」という絶対的な範囲（しきい値）を人間が固定で指定します。
メリット: 非常に直感的です。「鮮やかな緑だけ」や、逆に「熟した赤いトマトだけ」など、狙った色を正確に抜き出すことができます。
デメリット: 光の変化に非常に弱いです。例えば、晴れの日にピッタリ合うようにスライダーを調整しても、雲がかかって日陰になった瞬間に「明るさ（V）」が指定範囲から外れてしまい、植物を全く認識できなくなる（真っ黒になる）ことがあります。
2. ExG手法（環境光に自動で適応する）
ExG（Excess Green：拡張緑色指標）は、農業分野特有の数式手法です。ピクセルの色を「緑×2 － 赤 － 青」という計算式にかけて、「とにかく緑色が強い部分」を白く浮かび上がらせます。

仕組み: 浮かび上がらせた後に「大津の二値化」というアルゴリズムを組み合わせるのが一般的です（今回のシステムもそうなっています）。大津の二値化は、その時の画像の明るさに合わせて、自動で最適な白黒の境界線を引いてくれます。
メリット: 日向や日陰などの環境光の変化にめちゃくちゃ強いです。太陽の光が変わっても、自動でしきい値を計算し直すため、安定して植物（緑色）と土（茶色）を分離し続けてくれます。GUIのスライダー（Offset）は、その自動計算された基準線に対する微調整としてのみ機能します。
デメリット: 「緑色」を抽出することに特化しているため、赤い実や黄色い花などを抽出することには使えません。また、土の表面に緑色のコケが生えていると、それも植物として抽出してしまいます。
💡 現場での使い分けの目安
ExG： ドローンや屋外の自動走行ロボットなど、太陽光がコロコロ変わる屋外の畑で、雑草や作物の葉の面積（生育状況）を調べる場合。
HSV： 工場内や密閉型の植物工場など、常に一定のLED照明が当たっている環境で、特定の色だけを厳密に検査したい場合。


config/mosquitto.conf というファイルに allow_anonymous true（誰でもパスワードなしで接続OK）と設定しています。 この状態だと、同じWi-Fiや社内ネットワークに繋がっている人なら誰でも、MQTTソフトを使って画像や数値を盗み見ることができてしまいます。






#MQTTで出力するファイル
{
  "filename": "camera1_001.jpg",
  "mode": "hsv",
  "result_value": 45.2,
  "image_b64": "/9j/4AAQSkZJRgABAgAAAQABAAD..."
}
1. filename（処理したファイル名）
先ほど追加した機能です。どの画像の処理結果かが分かるように、元のファイル名を出力します。

2. mode（使用した手法）
「hsv」「exg」「yolo」のいずれかを出力します。GUIで選択されていたモードです。

3. result_value（診断の数値結果）★一番重要
モードによって出力される数値の意味が変わります。

HSV / ExG の場合: 画像全体のうち、**植物（指定した色）が占める面積の割合（％）**を出力します。（例：45.2 なら画像全体の45.2%が緑色） → 「作物がどれくらい成長したか」「雑草がどれくらい生えているか」の指標になります。
YOLO の場合: AIが**発見した物体の数（個数）**を出力します。（例：3 なら対象物が3つ見つかった） → 「実が何個なっているか」「害虫が何匹いるか」のカウントになります。
4. image_b64（処理後の画像データ）
抽出した部分だけを残して背景を真っ黒にした画像（HSV/ExG）や、AIが四角い枠（バウンディングボックス）を描き込んだ画像（YOLO）を、Base64という文字列データに変換して出力しています。 → 別のシステム（Node-REDやWebアプリ等）でこれを受信すれば、画面上に処理済みの写真をそのまま表示させることができます。


!!!!!!!!!!!!!!!!!!!!!!
画像処理後の画像削除機能は現在コメントアウト中